var assert = require("assert"),
    generator = require('../index');

describe('# Simple unit tests', function () {
    it('first simple unit test', function () {
        assert.equal(generator(), 'MongoDG - MongoDB Data Generator');
    });

    it('second simple unit test', function () {
        assert.notEqual(1, 2);
    });
});
